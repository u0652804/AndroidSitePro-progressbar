<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- define style : customer progressbar -->
    <style name="CustomProgressBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/custom_progressbar_horizontal</item>
        <item name="android:minHeight">12dip</item>
        <item name="android:maxHeight">24dip</item>
        <item name="android:progressBackgroundTint" tools:targetApi="lollipop">@color/colorWhite</item>
        <item name="android:progressTint" tools:targetApi="lollipop">@color/colorPrimary</item>
    </style>

</resources>
